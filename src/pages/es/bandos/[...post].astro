---
import SeoDetail from '../../../components/seo/SeoDetail.astro';

const { post } = Astro.params;
const slug = Array.isArray(post) ? post.join('/') : post ?? 'bando';
---

<SeoDetail locale="es" translationKey="seoRoutes.notices.detail" slug={slug} />
