---
import SeoDetail from '../../../components/seo/SeoDetail.astro';

const { post } = Astro.params;
const slug = Array.isArray(post) ? post.join('/') : post ?? 'noticia';
---

<SeoDetail locale="es" translationKey="seoRoutes.news.detail" slug={slug} />
